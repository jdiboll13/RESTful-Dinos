<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <link rel="stylesheet" href="/style.css">
    <title>Dinosuars!!</title>
  </head>
  <body>
    <div class="container">
      <h1>Look at all these <del>chickens</del> dinosaurs!</h1>
      <div class="dinos">
        <ul class="dinoInfo">
          {{#rows}}
            <li>
              <img src="{{imageurl}}" alt="oopps">
              <h2><a href="/api/dinosaurs/{{id}}">{{name}}</a></h2>
              <p>Length | {{length}}</p>
              <p>Weight | {{weight}}</p>
              <p>Habitat | {{habitat}}</p>
            </li>
          {{/rows}}
        </ul>
      </div>
    </div>
  </body>
</html>
